# ml_shadow_ab Plugin Configuration
name: ml_shadow_ab
category: ml
version: "1.0.0"
author: "OptiFIRE"
description: "Shadow A/B testing"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
