# sl_optuna_pruner Plugin Configuration
name: sl_optuna_pruner
category: ml
version: "1.0.0"
author: "OptiFIRE"
description: "Optuna hyperparameter pruning"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
