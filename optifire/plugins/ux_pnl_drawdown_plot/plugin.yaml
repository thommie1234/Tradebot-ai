# ux_pnl_drawdown_plot Plugin Configuration
name: ux_pnl_drawdown_plot
category: ux
version: "1.0.0"
author: "OptiFIRE"
description: "P&L and drawdown plots"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
