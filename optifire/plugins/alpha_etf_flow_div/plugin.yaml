# alpha_etf_flow_div Plugin Configuration
name: alpha_etf_flow_div
category: alpha
version: "1.0.0"
author: "OptiFIRE"
description: "ETF flow divergence detection"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
