# extra_bidask_filter Plugin Configuration
name: extra_bidask_filter
category: exec
version: "1.0.0"
author: "OptiFIRE"
description: "Bid-ask spread filter"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
