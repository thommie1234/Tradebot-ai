# diag_param_sensitivity Plugin Configuration
name: diag_param_sensitivity
category: diag
version: "1.0.0"
author: "OptiFIRE"
description: "Parameter sensitivity analysis"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
