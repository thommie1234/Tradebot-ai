# ml_onnx_runtime Plugin Configuration
name: ml_onnx_runtime
category: infra
version: "1.0.0"
author: "OptiFIRE"
description: "ONNX runtime inference"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
