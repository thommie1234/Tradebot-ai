# ai_dtw_matcher Plugin Configuration
name: ai_dtw_matcher
category: ai
version: "1.0.0"
author: "OptiFIRE"
description: "Dynamic Time Warping pattern matching"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
