# fe_duckdb_store Plugin Configuration
name: fe_duckdb_store
category: fe
version: "1.0.0"
author: "OptiFIRE"
description: "DuckDB feature store"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
