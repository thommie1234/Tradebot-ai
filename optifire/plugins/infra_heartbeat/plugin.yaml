# infra_heartbeat Plugin Configuration
name: infra_heartbeat
category: infra
version: "1.0.0"
author: "OptiFIRE"
description: "System heartbeat"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
