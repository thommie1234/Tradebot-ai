# ux_strategy_pie Plugin Configuration
name: ux_strategy_pie
category: ux
version: "1.0.0"
author: "OptiFIRE"
description: "Strategy allocation pie chart"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
