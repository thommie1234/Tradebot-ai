# ai_meta_labeling Plugin Configuration
name: ai_meta_labeling
category: ai
version: "1.0.0"
author: "OptiFIRE"
description: "Meta-labeling for bet sizing"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
