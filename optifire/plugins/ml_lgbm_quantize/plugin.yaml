# ml_lgbm_quantize Plugin Configuration
name: ml_lgbm_quantize
category: ml
version: "1.0.0"
author: "OptiFIRE"
description: "LightGBM quantization"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
