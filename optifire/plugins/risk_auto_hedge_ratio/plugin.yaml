# risk_auto_hedge_ratio Plugin Configuration
name: risk_auto_hedge_ratio
category: risk
version: "1.0.0"
author: "OptiFIRE"
description: "Auto hedge ratio calculation"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
