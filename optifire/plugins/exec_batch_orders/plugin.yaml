# exec_batch_orders Plugin Configuration
name: exec_batch_orders
category: exec
version: "1.0.0"
author: "OptiFIRE"
description: "Batch order execution"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
