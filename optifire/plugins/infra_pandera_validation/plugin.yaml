# infra_pandera_validation Plugin Configuration
name: infra_pandera_validation
category: infra
version: "1.0.0"
author: "OptiFIRE"
description: "Pandera data validation"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
