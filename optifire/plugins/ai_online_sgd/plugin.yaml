# ai_online_sgd Plugin Configuration
name: ai_online_sgd
category: ai
version: "1.0.0"
author: "OptiFIRE"
description: "Online SGD model updates"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
