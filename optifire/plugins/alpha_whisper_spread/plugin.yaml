# alpha_whisper_spread Plugin Configuration
name: alpha_whisper_spread
category: alpha
version: "1.0.0"
author: "OptiFIRE"
description: "Earnings whisper vs estimate spread"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
