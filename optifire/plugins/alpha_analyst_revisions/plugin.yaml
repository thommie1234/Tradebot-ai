# alpha_analyst_revisions Plugin Configuration
name: alpha_analyst_revisions
category: alpha
version: "1.0.0"
author: "OptiFIRE"
description: "Analyst revision momentum"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
