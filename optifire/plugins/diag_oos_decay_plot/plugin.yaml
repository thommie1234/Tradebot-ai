# diag_oos_decay_plot Plugin Configuration
name: diag_oos_decay_plot
category: diag
version: "1.0.0"
author: "OptiFIRE"
description: "Out-of-sample decay diagnostics"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
