# ux_discord_cmds Plugin Configuration
name: ux_discord_cmds
category: ux
version: "1.0.0"
author: "OptiFIRE"
description: "Discord bot commands"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
