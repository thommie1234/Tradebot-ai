# fe_wavelet Plugin Configuration
name: fe_wavelet
category: fe
version: "1.0.0"
author: "OptiFIRE"
description: "Wavelet decomposition features"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
