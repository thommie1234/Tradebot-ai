# infra_apscheduler Plugin Configuration
name: infra_apscheduler
category: infra
version: "1.0.0"
author: "OptiFIRE"
description: "APScheduler integration"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
