# fe_dollar_bars Plugin Configuration
name: fe_dollar_bars
category: fe
version: "1.0.0"
author: "OptiFIRE"
description: "Dollar bars construction"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
