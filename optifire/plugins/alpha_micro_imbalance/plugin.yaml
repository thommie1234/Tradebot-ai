# alpha_micro_imbalance Plugin Configuration
name: alpha_micro_imbalance
category: alpha
version: "1.0.0"
author: "OptiFIRE"
description: "Microstructure imbalance signals"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
