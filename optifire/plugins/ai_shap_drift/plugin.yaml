# ai_shap_drift Plugin Configuration
name: ai_shap_drift
category: ai
version: "1.0.0"
author: "OptiFIRE"
description: "SHAP-based feature drift detection"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
