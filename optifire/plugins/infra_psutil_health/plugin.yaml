# infra_psutil_health Plugin Configuration
name: infra_psutil_health
category: infra
version: "1.0.0"
author: "OptiFIRE"
description: "Psutil health monitoring"

# Resource budgets
budget:
  cpu_ms: 1000
  mem_mb: 50

# Execution schedule (cron, @idle, @open, @close, interval_Xs)
schedule: "@idle"

# Dependencies
dependencies: []

# Enabled by default
enabled: true
